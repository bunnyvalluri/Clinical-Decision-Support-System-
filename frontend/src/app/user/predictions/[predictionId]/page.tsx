"use client";

import * as React from "react";
import Link from "next/link";
import { useParams, useRouter } from "next/navigation";
import {
  ArrowLeft,
  TrendingUp,
  ShieldCheck,
  AlertTriangle,
  Calendar,
  MessageSquare,
  Activity,
  HeartPulse,
} from "lucide-react";
import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";

export default function PatientPredictionDetailPage() {
  const params = useParams();
  const router = useRouter();
  const predictionId = params?.predictionId as string;

  return (
    <div className="p-6 space-y-6 max-w-4xl mx-auto">
      <div className="flex items-center gap-3">
        <Button
          variant="ghost"
          size="sm"
          onClick={() => router.push("/user/predictions")}
          className="text-xs gap-1.5"
        >
          <ArrowLeft className="h-4 w-4" /> Back to Predictions
        </Button>
      </div>

      <div className="bg-white p-6 rounded-2xl border border-slate-200 shadow-sm space-y-6">
        {/* Header */}
        <div className="flex flex-col sm:flex-row sm:items-center justify-between gap-4 border-b border-slate-100 pb-4">
          <div>
            <div className="flex items-center gap-2">
              <Badge className="bg-amber-50 text-amber-800 border-amber-200 text-xs font-bold">
                MODERATE RISK ESTIMATE
              </Badge>
              <span className="text-xs font-mono text-slate-400">ID: {predictionId}</span>
            </div>
            <h1 className="text-xl font-bold text-slate-900 mt-1">
              Cardiovascular Health Risk Estimate
            </h1>
            <p className="text-xs text-slate-500">
              Generated by CardioEnsemble-RF v1.4.2 · Supervised by Dr. Elena Vance, MD
            </p>
          </div>

          <div className="flex items-center gap-2">
            <Link href="/user/appointments">
              <Button size="sm" className="bg-teal-600 hover:bg-teal-700 text-white text-xs gap-1.5 shadow-sm">
                <Calendar className="h-3.5 w-3.5" /> Book Provider Review
              </Button>
            </Link>
          </div>
        </div>

        {/* Probability Metric & Confidence Interval */}
        <div className="grid grid-cols-1 sm:grid-cols-3 gap-4">
          <div className="p-4 bg-slate-50 rounded-xl border border-slate-100 space-y-1">
            <span className="text-xs text-slate-500 font-medium">Estimated Probability</span>
            <p className="text-2xl font-bold text-slate-900">42.0%</p>
            <p className="text-[11px] text-amber-700 font-medium">Moderate Clinical Risk Tier</p>
          </div>

          <div className="p-4 bg-slate-50 rounded-xl border border-slate-100 space-y-1">
            <span className="text-xs text-slate-500 font-medium">95% Confidence Interval</span>
            <p className="text-xl font-bold text-slate-900 font-mono">[36.0% – 48.0%]</p>
            <p className="text-[11px] text-slate-400">Statistically Calibrated</p>
          </div>

          <div className="p-4 bg-slate-50 rounded-xl border border-slate-100 space-y-1">
            <span className="text-xs text-slate-500 font-medium">Clinical Supervision</span>
            <p className="text-sm font-bold text-emerald-700 flex items-center gap-1.5 mt-1">
              <ShieldCheck className="h-4 w-4" /> Physician Reviewed
            </p>
            <p className="text-[11px] text-slate-500">Dr. Elena Vance, MD</p>
          </div>
        </div>

        {/* Key Factor Contributions */}
        <div className="space-y-3">
          <h3 className="text-xs font-bold text-slate-900 uppercase tracking-wider">
            Key Contributing Health Factors
          </h3>
          <p className="text-xs text-slate-500">
            How your reported measurements influenced this risk estimation:
          </p>

          <div className="space-y-2">
            {[
              { factor: "Resting Systolic Blood Pressure (136 mmHg)", impact: "Moderate positive risk factor", weight: 65, color: "bg-amber-500" },
              { factor: "Age Category (68 years)", impact: "Standard age-adjusted baseline factor", weight: 50, color: "bg-teal-600" },
              { factor: "Serum Cholesterol (210 mg/dl)", impact: "Slight elevation above target 200 mg/dl", weight: 40, color: "bg-amber-400" },
              { factor: "Absence of Exertional Angina", impact: "Protective factor decreasing risk", weight: 30, color: "bg-emerald-500" },
            ].map(({ factor, impact, weight, color }) => (
              <div key={factor} className="p-3 bg-slate-50 rounded-xl border border-slate-100 space-y-1.5">
                <div className="flex justify-between text-xs">
                  <span className="font-semibold text-slate-800">{factor}</span>
                  <span className="text-slate-500 text-[11px]">{impact}</span>
                </div>
                <div className="h-2 w-full bg-slate-200 rounded-full overflow-hidden">
                  <div className={`h-full rounded-full ${color}`} style={{ width: `${weight}%` }} />
                </div>
              </div>
            ))}
          </div>
        </div>

        {/* Mandatory Clinical Safety Box */}
        <div className="p-4 rounded-xl bg-amber-50 border border-amber-200 flex items-start gap-3">
          <AlertTriangle className="h-5 w-5 text-amber-600 shrink-0 mt-0.5" />
          <div className="space-y-1 text-xs text-amber-900 leading-relaxed">
            <p className="font-bold">Medical Decision Support Notice:</p>
            <p>
              The model estimates an increased risk based on the information provided. <strong>This prediction is not a diagnosis.</strong> It is generated by a clinical decision support algorithm designed to assist you and your healthcare team. Please discuss this result with your healthcare professional before making changes to your medication or diet.
            </p>
          </div>
        </div>

        <div className="flex items-center justify-between pt-2 border-t border-slate-100">
          <Link href="/user/messages">
            <Button variant="outline" size="sm" className="text-xs gap-1.5 border-slate-200 text-slate-700">
              <MessageSquare className="h-3.5 w-3.5" /> Message Care Team About This Result
            </Button>
          </Link>
          <span className="text-[11px] text-slate-400 font-mono">Algorithm: Random Forest v1.4.2</span>
        </div>
      </div>
    </div>
  );
}
