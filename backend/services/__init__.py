"""
Services package.

Business logic lives here, NOT in views or models.
Each service class handles a specific domain area and depends on
repositories (data access) rather than directly on Django ORM calls.
"""
