{
  "name": "clinical-decision-support-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm run start",
    "specs:validate": "python scripts/validate_specs.py",
    "specs:converge": "python scripts/converge.py"
  },
  "dependencies": {
    "@neon/config": "^1.4.2",
    "@neon/env": "^1.3.2"
  }
}
