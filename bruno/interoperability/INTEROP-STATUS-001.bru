meta {
  name: INTEROP-STATUS-001
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/v1/interoperability/status/
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{informaticist_access_token}}
}

assert {
  res.status: eq 200
  res.body.has_data: isDefined
}
