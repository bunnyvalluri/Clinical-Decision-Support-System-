meta {
  name: List Loop Budgets
  type: http
  seq: 4
}

get {
  url: {{base_url}}/api/v1/engineering/budgets/
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{admin_access_token}}
}

assert {
  res.status: eq 200
}
