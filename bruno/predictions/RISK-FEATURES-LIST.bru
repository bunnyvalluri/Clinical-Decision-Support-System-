meta {
  name: RISK-FEATURES-LIST
  type: http
  seq: 12
}

get {
  url: {{base_url}}/api/v1/risk/features/
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{doctor_access_token}}
}

assert {
  res.status: eq 200
}
