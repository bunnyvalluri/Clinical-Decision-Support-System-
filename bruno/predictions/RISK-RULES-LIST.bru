meta {
  name: RISK-RULES-LIST
  type: http
  seq: 13
}

get {
  url: {{base_url}}/api/v1/risk/rules/
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{doctor_access_token}}
}

assert {
  res.status: eq 200
}
