meta {
  name: CLINE-KILL-SWITCH-001
  type: http
  seq: 6
}

get {
  url: {{base_url}}/api/v1/ai/cline/kill-switch/
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{admin_access_token}}
}

assert {
  res.status: in [200, 401, 403]
}
