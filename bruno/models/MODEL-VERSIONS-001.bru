meta {
  name: MODEL-VERSIONS-001
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/v1/models/versions/
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{informaticist_access_token}}
}

assert {
  res.status: in [200, 401]
}
