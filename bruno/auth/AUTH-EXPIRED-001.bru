meta {
  name: AUTH-EXPIRED-001
  type: http
  seq: 5
}

get {
  url: {{base_url}}/api/v1/auth/me/
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: invalid_or_expired_jwt_token_format
}

assert {
  res.status: eq 401
}
